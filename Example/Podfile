
platform :ios, '8.0'

use_frameworks!

target 'GLTools_Example' do
    use_frameworks!
  pod 'GLTools', :path => '../'
  pod 'SnapKit', '~> 4.0.0'
  pod 'Kingfisher', '~> 4.0.0'
  use_frameworks!
  target 'GLTools_Tests' do
    inherit! :search_paths

    pod 'FBSnapshotTestCase' , '~> 2.1.4'
    use_frameworks!
  end
end
